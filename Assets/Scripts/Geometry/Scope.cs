using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SchoolAdventure.Games.Geometrie
{
    public class Scope : MonoBehaviour
    {
        // Start is called before the first frame update
        /*void Start()
        {
            //Cursor. visible = false;
        }

        // Update is called once per frame
        void Update()
        {
            //Vector3 pos = this.GetComponent<Camera>().ScreenToWorldPoint(Input.mousePosition);
            //Vector3 pos = this.GetComponent<Camera>().ScreenToWorldPoint(InputManager.GetInstance().GetRawMousePosition());
            Vector3 pos = InputManager.GetInstance().GetMousePosition();
            pos.z = -9;
            //var target = GameObject.Find("scope");
            //target.transform.position = pos;
        }*/
    }
}
